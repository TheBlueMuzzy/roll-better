---
phase: 09-multi-player-display
plan: 01
type: execute
---

<objective>
Restructure the game board layout to accommodate player profile groups and prepare for multi-player visual display.

Purpose: Create the foundational layout changes that all other Phase 9 plans depend on — scaled dice, shifted rows, and profile group components for every player + the goal row.
Output: PlayerProfileGroup and GoalProfileGroup components, restructured row layout with avatar space on the left.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-multi-player-display/CONTEXT.md
@.planning/phases/04-game-board-layout/04-02-SUMMARY.md
@.planning/phases/06-lerp-animation/06-01-SUMMARY.md
@.planning/phases/08-ai-opponents/08-02-SUMMARY.md

@src/components/GoalRow.tsx
@src/components/PlayerRow.tsx
@src/components/PlayerIcon.tsx
@src/components/Scene.tsx
@src/components/RollingArea.tsx
@src/components/Die3D.tsx
@src/types/game.ts
@src/store/gameStore.ts

**Constraining decisions:**
- GoalRow Z = -4.67, PlayerRow Z = -3.77, AI rows offset +0.9 each
- SLOT_SPACING = 0.7, SLOT_COUNT = 8, DIE_SIZE = arena_width / 8.5 ≈ 0.659
- PlayerIcon currently uses drei Html overlay at lower-left of rolling area
- PLAYER_COLORS defined in gameStore.ts (array) and Die3D.tsx (object)
- AI player rows currently rendered as plain PlayerRow components in Scene.tsx

**Design direction (from CONTEXT.md):**
- Scale all dice down slightly so an additional die COULD fit in the row (breathing room)
- Shift locked dice rows to the right, preserving the current right-side gap
- Freed-up space on the left: avatar/profile group for each player
- Profile group: avatar circle (placeholder) + points (large) + starting dice (small)
- Goal row: star-in-circle instead of avatar, same left-side position
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scale dice and shift rows for avatar space</name>
  <files>src/components/GoalRow.tsx, src/components/RollingArea.tsx, src/components/PlayerRow.tsx</files>
  <action>
Reduce SLOT_SPACING from 0.7 to ~0.62 (so 9 slots worth of space fits in the same total width). Reduce DIE_SIZE proportionally — change the divisor from 8.5 to ~9.5 so dice are slightly smaller. These two constants drive all slot positioning.

Shift the row origin: currently getSlotX centers 8 slots around X=0. Add an X offset so the 8 slots shift rightward, leaving ~1 slot's worth of space on the left for the profile group. The right edge should stay roughly where it is now. Approach: offset = SLOT_SPACING * 0.5 (half a slot right), applied in getSlotX. Or adjust the centering formula from (i - 3.5) to (i - 3.0) to shift right.

Both GoalRow and PlayerRow use getSlotX — changing it once shifts both rows. DIE_SIZE is exported from RollingArea and imported everywhere.

Do NOT change Z positions, wall sizes, or arena dimensions. Only scale + horizontal shift.
  </action>
  <verify>npm run build passes. Visual inspection: dice are slightly smaller, row has space on the left, right edge roughly unchanged.</verify>
  <done>SLOT_SPACING reduced, DIE_SIZE reduced, getSlotX shifted right. Both GoalRow and PlayerRow render with breathing room and left-side space.</done>
</task>

<task type="auto">
  <name>Task 2: Create PlayerProfileGroup and GoalProfileGroup components</name>
  <files>src/components/PlayerProfileGroup.tsx, src/components/GoalProfileGroup.tsx, src/components/Scene.tsx, src/components/PlayerIcon.tsx</files>
  <action>
**PlayerProfileGroup** — new component, drei Html overlay (same pattern as PlayerIcon):
- Avatar circle: 36px diameter, filled with player color, border-radius 50%. Placeholder for future profile images.
- Below/overlapping avatar: points (larger text, ~18px bold) and starting dice count (smaller, ~12px, secondary color).
- Props: name, color, score, startingDice, position (3D world coords).
- Render one per player (human + all AI) positioned to the LEFT of each player's row. X position = leftmost slot X minus ~0.7 (one slot width left of the row). Z position = same as that player's row Z. Y = 0.
- The human player's group is at their row Z (-3.77). Each AI player's group is at their row Z (-3.77 + (idx+1) * 0.9).

**GoalProfileGroup** — new component, drei Html overlay:
- Star icon inside a circle: 36px diameter circle, gold/yellow fill, star shape inside (use CSS or unicode ★).
- Positioned to the left of the goal row, same X offset as player profiles, Z = goal row Z (-4.67).

**Wire into Scene.tsx:**
- Replace the current PlayerIcon with PlayerProfileGroup for the human player.
- Add PlayerProfileGroup for each AI player.
- Add GoalProfileGroup to the left of GoalRow.
- Remove the old PlayerIcon component usage (or refactor it — the profile group replaces it).

Keep the handicap Z scale-pop animation from PlayerIcon — move it into PlayerProfileGroup or keep PlayerIcon as a sub-component inside the profile group.

Do NOT remove the HUD (score display, roll prompt, etc.) — that stays as a separate HTML overlay.
  </action>
  <verify>npm run build passes. Dev server shows profile groups next to each row — human, AI, and goal.</verify>
  <done>PlayerProfileGroup renders for every player with avatar circle + stats. GoalProfileGroup renders with star icon. All positioned left of their respective rows. Old PlayerIcon replaced or refactored.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Restructured layout with smaller dice, shifted rows, and profile groups for all players + goal row</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit the dev server URL
    3. Check: Dice are slightly smaller with breathing room in the row
    4. Check: Rows are shifted right, space on the left for profile groups
    5. Check: Human player has profile group (avatar circle + score + starting dice) to the left of their row
    6. Check: Each AI player has a profile group to the left of their row
    7. Check: Goal row has a star-in-circle icon to its left
    8. Check: Game still plays correctly (roll, lock, unlock, round transitions)
    9. Check: No layout overlap or clipping issues
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] No TypeScript errors
- [ ] Dice are visibly smaller with row breathing room
- [ ] Profile groups visible for all players and goal row
- [ ] Game loop still functions (roll → lock → unlock → score → round end)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Layout restructured with avatar space
- Profile groups rendering for all players + goal
  </success_criteria>

<output>
After completion, create `.planning/phases/09-multi-player-display/09-01-SUMMARY.md`
</output>
