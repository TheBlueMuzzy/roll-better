---
phase: 22-main-menu-restructure
plan: 01
type: execute
---

<objective>
Simplify the main menu by removing the difficulty selector, adding How to Play and Upgrades buttons.

Purpose: Clean up the menu for v1.2 — difficulty will be randomized per-AI in Phase 24, How to Play deserves main menu visibility, and Upgrades is a placeholder for future cosmetics/progression.
Output: Restructured MainMenu with cleaner layout — title, player count, PLAY, PLAY ONLINE, How to Play, Upgrades (coming soon), Settings.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/MainMenu.tsx
@src/components/HowToPlay.tsx
@src/components/Settings.tsx
@src/App.tsx
@src/App.css

**Constraining decisions:**
- Phase 24 will randomize AI difficulty per-AI at fill time — Phase 22 just removes the UI selector
- Phase 23 will rework Settings (gear icon, shake-to-roll removal) — leave Settings.tsx alone here
- Keep How to Play in Settings too (Phase 23 will clean that up) — just ADD it to main menu

**Tech available:** React 18, TypeScript, Zustand, CSS (no CSS modules — single App.css)
**Established patterns:** menu-btn class for buttons, menu-selector-group for grouped options, showClass animation pattern for fade-in
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure MainMenu component and wire How to Play</name>
  <files>src/components/MainMenu.tsx, src/App.tsx</files>
  <action>
  In MainMenu.tsx:
  - Remove the DIFFICULTIES constant and difficulty-related state (useState for difficulty, useEffect sync for difficulty)
  - Remove the difficulty selector group JSX (the entire menu-selector-group block for Difficulty)
  - Remove difficulty from the onPlay callback — change signature to `onPlay: (playerCount: number) => void`
  - In the PLAY button onClick, call `onPlay(playerCount)` (no difficulty arg). Update setGamePrefs to only set playerCount.
  - Add `onOpenHowToPlay: () => void` prop to MainMenuProps
  - Add a "How to Play" button after the PLAY ONLINE button, styled with className "menu-link-btn". Use the book emoji and text: `{'\u{1F4D6}'} How to Play`. onClick calls playUIClick() then onOpenHowToPlay().
  - Add an "Upgrades" button after How to Play, styled with className "menu-link-btn menu-upgrades-btn" and disabled={true}. Text: `Upgrades`. Add a small "Coming Soon" span below/inside with className "menu-coming-soon-label".
  - Keep the Settings link as-is (it will be reworked in Phase 23)

  In App.tsx:
  - Update handlePlay to remove the difficulty parameter — use 'medium' as default: `const handlePlay = useCallback((playerCount: number) => { ... }` and pass 'medium' where difficulty was used in initGame
  - Pass `onOpenHowToPlay={() => { playUIClick(); setHowToPlayOpen(true); }}` to MainMenu (check if howToPlayOpen state and HowToPlay component are already wired in App.tsx — if not, add useState for it and render HowToPlay conditionally)
  - Verify the AIDifficulty type import is still used elsewhere before removing
  </action>
  <verify>TypeScript compiles without errors: `npx tsc --noEmit`</verify>
  <done>MainMenu renders without difficulty selector. PLAY button works with default medium difficulty. How to Play button opens the HowToPlay carousel. Upgrades button is visible but disabled.</done>
</task>

<task type="auto">
  <name>Task 2: Update CSS for restructured menu layout</name>
  <files>src/App.css</files>
  <action>
  Add styles for the new menu buttons:

  .menu-link-btn — styled similarly to .menu-settings-link but slightly more prominent:
  - background: none, border: none
  - color: rgba(255, 255, 255, 0.6) (brighter than settings link's 0.4)
  - font-size: var(--fs-sm) or slightly larger
  - font-family: inherit
  - padding: 8px 16px
  - cursor: pointer
  - Add a subtle hover/active state

  .menu-upgrades-btn — additional styles for disabled state:
  - When disabled: opacity 0.4, cursor: default
  - pointer-events: none when disabled (prevent click)

  .menu-coming-soon-label — small label:
  - display: block
  - font-size: 10px
  - color: rgba(255, 255, 255, 0.3)
  - margin-top: 2px
  - text-transform: uppercase
  - letter-spacing: 1px

  Add spacing between the link buttons — use a wrapper div with className "menu-links" if needed, with gap or margin between children. Place How to Play + Upgrades + Settings in this group at the bottom of the menu.

  Keep existing .menu-coming-soon class (may still be referenced elsewhere).
  </action>
  <verify>Visual inspection — menu renders with proper spacing, disabled Upgrades button looks intentionally grayed out, How to Play button is readable and tappable</verify>
  <done>All new menu elements styled consistently with existing menu aesthetic. No layout overflow on mobile viewport (375px width). Disabled Upgrades button visually distinct from active buttons.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Restructured main menu — removed difficulty selector, added How to Play and Upgrades buttons</what-built>
  <how-to-verify>
    1. Run: npm run dev (or dev server should already be running)
    2. Open the game in browser
    3. Main menu should show: title, player count selector (2/3/4), PLAY, PLAY ONLINE, How to Play, Upgrades (grayed out, "Coming Soon"), Settings
    4. Verify: NO difficulty selector (Easy/Medium/Hard) visible
    5. Tap "How to Play" — carousel modal should open with slides
    6. Close carousel, tap PLAY with any player count — game should start normally (AI uses medium difficulty by default)
    7. Check mobile viewport (375px) — no overflow, everything fits
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run build` succeeds
- [ ] Main menu shows correct layout (no difficulty selector, HTP + Upgrades visible)
- [ ] How to Play opens from main menu
- [ ] PLAY starts game with AI opponents (medium difficulty default)
- [ ] PLAY ONLINE still navigates to lobby
- [ ] No console errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Difficulty selector removed from main menu
- How to Play accessible from main menu
- Upgrades placeholder visible with "Coming Soon"
- Game still plays correctly with default difficulty
</success_criteria>

<output>
After completion, create `.planning/phases/22-main-menu-restructure/22-01-SUMMARY.md`
</output>
