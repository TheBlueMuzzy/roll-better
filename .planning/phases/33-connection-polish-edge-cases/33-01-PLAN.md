---
phase: 33-connection-polish-edge-cases
plan: 01
type: execute
---

<objective>
Reject stale tab connections and enforce one-connection-per-player on the server.

Purpose: Prevent desync and ghost players when a user opens the same room in multiple tabs. The second tab should take over; the first tab gets a clear "connected elsewhere" message.
Output: Server detects duplicate persistentId on connect, closes old connection with typed error. Client handles the new error gracefully.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context (dependency graph):
@.planning/phases/27-player-identity-seat-model/27-01-SUMMARY.md
@.planning/phases/30-mid-game-join-flow/30-01-SUMMARY.md
@.planning/phases/31-host-migration-room-lifecycle/31-02-SUMMARY.md

# Key source files:
@party/server.ts
@src/types/protocol.ts
@src/hooks/useRoom.ts
@src/components/MainMenu.tsx

**Tech stack available:** PartyKit (PartyServer, Connection), React, Zustand
**Established patterns:** Typed error codes on ErrorMessage (code field), room_closed/room_full handling in useRoom, migrateHost/dissolveRoom helpers

**Constraining decisions:**
- 27-01: Dual identity model — conn.id (sessionStorage) for WebSocket session, persistentId (localStorage) for cross-session seat ownership
- 27-01: persistentIdToConnId map persists after disconnect for returning player detection
- 31-02: Typed error codes on ErrorMessage (code field); client switches on code for UI behavior
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server duplicate-persistentId detection and old connection eviction</name>
  <files>party/server.ts, src/types/protocol.ts</files>
  <action>
In `onConnect` (and `handleJoin`), after extracting persistentId from the join message, check if another active connection already has the same persistentId:

1. In `handleJoin`, after getting `persistentId` from the message:
   - Look up `this.persistentIdToConnId.get(persistentId)` to find an existing connId
   - If an existing connId is found AND it's different from `conn.id` AND the old connection is still active (check `this.party.getConnection(oldConnId)`):
     - Send the OLD connection an error message: `{ type: "error", code: "connected_elsewhere", message: "Connected from another tab" }`
     - Close the old connection: `oldConn.close()`
     - Remove the old connId from `this.players` map (if in lobby)
     - Clean up old connId from `this.midGameJoiners` and `this.pendingSeatClaims` (if mid-game)
   - Then proceed normally with the new connection

2. Add `"connected_elsewhere"` to the documented error codes in protocol.ts (add a comment near ErrorMessage interface listing known codes: `room_full`, `connected_elsewhere`).

Do NOT change the persistentIdToConnId mapping logic — the new connection's mapping will overwrite naturally. Do NOT change any reconnect/rejoin logic — those operate on conn.id matching, which is different from this persistentId duplicate check.

Handle the edge case where old connection was in-game (a player in this.players with gameState): the old connection should be treated as a disconnect (trigger grace timer / bot promotion as normal via removePlayer), then the new connection proceeds through normal join flow which will detect the returning player via persistentId.
  </action>
  <verify>
Review the logic: opening two tabs with the same room code should close the first tab's connection with "connected_elsewhere" error. The second tab joins normally.
  </verify>
  <done>
Server detects duplicate persistentId across connections, evicts old connection with typed error code, new connection proceeds normally. In-game eviction triggers standard disconnect handling (grace timer → bot promotion path).
  </done>
</task>

<task type="auto">
  <name>Task 2: Client handles connected_elsewhere error gracefully</name>
  <files>src/hooks/useRoom.ts, src/components/MainMenu.tsx, src/App.css</files>
  <action>
1. In useRoom.ts `onmessage` error handler, add a case for `code === "connected_elsewhere"`:
   - Set a new state field `connectedElsewhere: true` (add to the hook's state)
   - Do NOT auto-clear this error (same pattern as room_full — user needs to see it)
   - Call `leave()` to clean up the socket

2. In MainMenu.tsx, when `room.connectedElsewhere` is true, show a message:
   - "You're connected in another tab"
   - Show a "BACK TO MENU" button that resets onlineMode to idle and clears the error
   - This should take priority over other online states (show it above the join/create flow)

3. In useRoom.ts, also handle the WebSocket `onclose` event — if the close came from server (not user-initiated leave), and we haven't received a connected_elsewhere error, leave existing behavior. The connected_elsewhere error message arrives BEFORE the close, so the flag will already be set.

4. Add minimal CSS for the connected_elsewhere state (reuse existing error styling patterns from room_full).

Do NOT add reconnect attempts when connected_elsewhere — the user should go back to menu and use the other tab.
  </action>
  <verify>Build passes: `npm run build`. Review that connected_elsewhere state is handled in both useRoom and MainMenu without breaking existing error flows.</verify>
  <done>Client shows "You're connected in another tab" with BACK TO MENU button. No reconnect attempts. Error doesn't auto-clear.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] No TypeScript errors
- [ ] Duplicate persistentId detection only fires when old connection is still active
- [ ] In-game duplicate triggers standard disconnect path (not data loss)
- [ ] Client shows clear "connected elsewhere" message
- [ ] Existing reconnect flow (same conn.id returning) is NOT broken
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Stale tab gets evicted with clear error message
- New tab joins normally (lobby or mid-game)
- In-game eviction follows standard disconnect → grace timer → bot path
</success_criteria>

<output>
After completion, create `.planning/phases/33-connection-polish-edge-cases/33-01-SUMMARY.md`
</output>
