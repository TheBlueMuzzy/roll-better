---
phase: 18-unlock-scoring-sync
plan: 01
type: execute
---

<objective>
Handle server scoring and session end messages on the client — update player scores, show scoring animations, and transition to winners screen when session ends.

Purpose: Complete the scoring feedback loop so online players see who won each round, the score counting animation, and the final winners screen — matching the local game experience exactly.
Output: Working online scoring phase (score animation, HUD display) and session end (winners screen).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-unlock-scoring-sync/18-CONTEXT.md

# Key source files:
@src/hooks/useOnlineGame.ts
@src/store/gameStore.ts
@src/types/protocol.ts
@src/App.tsx
@src/components/HUD.tsx

# Prior phase summary:
@.planning/phases/17-dice-sync-simultaneous-play/17-04-SUMMARY.md

**Established patterns:**
- Per-player relay: server processes each player individually, broadcasts via broadcastExcept
- Buffered reveals: buffer incoming results, flush after local action completes
- Store actions for online data: applyOnlineUnlockResult, addPendingLockReveal patterns
- useOnlineGame.ts handles all server→client game messages via socket listener
- onlinePlayerIds[0] = local player's server ID (connection ID)

**Constraining decisions:**
- Phase 17: Client-authoritative dice, per-player relay, buffered reveals
- Server already computes scoring (handleScoring) and sends scoring message with winners[] and players[]
- Server already computes handicap and either sends session_end or phase_change: roundEnd → round_start
- HUD shows scoring phase with roundScore counting animation (triggered by phase === 'scoring' && roundScore > 0)
- Multi-winner: winners[] array in ScoringMessage (multiple players can win same roll)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Handle scoring message — apply server scores and trigger scoring phase</name>
  <files>src/store/gameStore.ts, src/hooks/useOnlineGame.ts</files>
  <action>
  1. In gameStore.ts, add a new store action `applyOnlineScoring`:
     - Signature: `applyOnlineScoring(winners: { playerId: string; roundScore: number }[], serverPlayers: PlayerSyncState[]) => void`
     - Import PlayerSyncState from protocol.ts (already partially imported)
     - Map server player data to local players using onlinePlayerIds:
       - For each local player at index i, find matching server player by onlinePlayerIds[i]
       - Update `score` from server data (server-authoritative)
     - Determine local player's roundScore: find onlinePlayerIds[0] in winners array. If found, use their roundScore. If not found, use 0.
     - Set phase to 'scoring' and roundState.roundScore to the local player's roundScore
     - This ensures the HUD counting animation triggers correctly

  2. In useOnlineGame.ts, replace the stubbed `scoring` handler:
     - Call `useGameStore.getState().applyOnlineScoring(msg.winners, msg.players)`
     - Log: `[useOnlineGame] scoring — winners: [names], local roundScore: N`

  Do NOT modify the server (party/server.ts) — scoring logic is already complete there.
  Do NOT touch the local scoring useEffect in App.tsx — it already returns early for online.
  </action>
  <verify>
  1. TypeScript compiles: `npx tsc --noEmit` passes
  2. grep for "Phase 18 scope" in useOnlineGame.ts — scoring case should no longer have that comment
  </verify>
  <done>applyOnlineScoring store action exists. scoring message handler calls it. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Handle session_end message — show winners screen</name>
  <files>src/hooks/useOnlineGame.ts, src/store/gameStore.ts</files>
  <action>
  1. In useOnlineGame.ts, replace the stubbed `session_end` handler:
     - Map server player data to local players (same mapping pattern as scoring: use onlinePlayerIds to match server IDs to local indices, update scores from server data)
     - Set phase to 'sessionEnd'
     - Set screen to 'winners'
     - Note: the existing WinnersScreen component reads player scores from the store, so updating scores before setting screen is sufficient
     - Use useGameStore.getState() for all store access (not hook selectors — this runs inside a message handler)

  2. Add a store action `applyOnlineSessionEnd(serverPlayers: PlayerSyncState[])` in gameStore.ts:
     - Map server player data to local players (update scores from server — same pattern as applyOnlineScoring but without roundScore/winners)
     - Set phase to 'sessionEnd'
     - Set screen to 'winners'

  3. In useOnlineGame.ts session_end handler, call `useGameStore.getState().applyOnlineSessionEnd(msg.players)`

  Do NOT add special online ceremony — context says just show existing winners screen.
  Do NOT handle play-again flow — that's existing menu → initGame flow.
  </action>
  <verify>
  1. TypeScript compiles: `npx tsc --noEmit` passes
  2. grep for "Phase 18 scope" in useOnlineGame.ts — session_end case should no longer have that comment
  </verify>
  <done>session_end handler implemented. applyOnlineSessionEnd store action exists. Winners screen displays with server-authoritative scores. TypeScript compiles.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes
- [ ] No "Phase 18 scope" comments remain for scoring or session_end in useOnlineGame.ts
- [ ] applyOnlineScoring and applyOnlineSessionEnd actions exist in gameStore interface and implementation
- [ ] Both handlers use onlinePlayerIds mapping to sync server→client player data
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- Scoring message updates player scores and triggers scoring phase with correct roundScore
- Session end message updates scores and shows winners screen
</success_criteria>

<output>
After completion, create `.planning/phases/18-unlock-scoring-sync/18-01-SUMMARY.md`
</output>
