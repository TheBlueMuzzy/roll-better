---
phase: 23-settings-controls-cleanup
plan: 01
type: execute
---

<objective>
Remove shake-to-roll feature entirely and clean up the Settings panel by removing the redundant How to Play button.

Purpose: Shake-to-roll is being removed per v1.2 scope decisions. How to Play was added to the main menu in Phase 22, making the Settings copy redundant.
Output: Cleaner Settings panel, reduced code surface, no shake-related code remaining.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/phase-prompt.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-main-menu-restructure/22-01-SUMMARY.md

# Key files (all modified or deleted in this plan):
@src/hooks/useShakeToRoll.ts
@src/hooks/useAccelerometerGravity.ts
@src/components/GravityController.tsx
@src/components/Settings.tsx
@src/components/HUD.tsx
@src/components/Scene.tsx
@src/App.tsx
@src/store/gameStore.ts
@src/types/game.ts

**Constraining decisions:**
- Phase 22: How to Play now accessible from main menu (redundant in Settings)
- v1.2 scope: Shake-to-roll being removed entirely
- v1.2 scope: Confirmation toggle stays (may be used in v1.3 drag-to-unlock)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove all shake-to-roll code</name>
  <files>
    src/hooks/useShakeToRoll.ts (DELETE),
    src/hooks/useAccelerometerGravity.ts (DELETE),
    src/components/GravityController.tsx (DELETE),
    src/App.tsx,
    src/components/HUD.tsx,
    src/components/Settings.tsx,
    src/components/Scene.tsx,
    src/store/gameStore.ts,
    src/types/game.ts
  </files>
  <action>
    1. DELETE src/hooks/useShakeToRoll.ts entirely
    2. DELETE src/hooks/useAccelerometerGravity.ts entirely
    3. DELETE src/components/GravityController.tsx entirely

    4. In src/types/game.ts:
       - Remove `shakeToRollEnabled` from the Settings interface (line 88)

    5. In src/store/gameStore.ts:
       - Remove `shakeToRollEnabled: true` from settings defaults (line 157)
       - Remove `setShakeToRollEnabled` from the store interface (line 89)
       - Remove the `setShakeToRollEnabled` implementation (lines 966-968)

    6. In src/components/Settings.tsx:
       - Remove `shakeSupported` from SettingsProps interface
       - Remove `shakeToRollEnabled` store selector
       - Remove `setShakeToRollEnabled` store selector
       - Remove the entire "Shake to Roll Toggle" section (lines 91-109)
       - Update component signature to remove shakeSupported param

    7. In src/components/HUD.tsx:
       - Remove `shakeEnabled` and `onRequestShakePermission` from HUDProps
       - Remove the "Enable Shake" button block (lines 208-212)
       - Change idle status text from conditional "Shake or Tap to Roll" / "Tap to Roll" to just "Tap to Roll" (line 154-155, remove isMobile check)

    8. In src/App.tsx:
       - Remove import of useShakeToRoll
       - Remove import of useAccelerometerGravity
       - Remove `shakeToRollEnabled` store selector (line 505)
       - Remove the useShakeToRoll call and its destructured results (lines 506-507)
       - Remove the useAccelerometerGravity call (line 510)
       - Remove `shakeEnabled` and `onRequestShakePermission` props from HUD (lines 564-565)
       - Remove `shakeSupported` prop from Settings component (line 586)

    9. In src/components/Scene.tsx:
       - Remove import of GravityController (line 4)
       - Remove <GravityController /> from JSX (line 382)
  </action>
  <verify>
    npm run build — no TypeScript errors, no missing imports, clean build
  </verify>
  <done>
    All shake-to-roll code deleted. No references to useShakeToRoll, useAccelerometerGravity, GravityController, shakeToRollEnabled, or shakeSupported remain in the codebase. Build passes clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove How to Play button from Settings panel</name>
  <files>src/components/Settings.tsx, src/App.css</files>
  <action>
    1. In src/components/Settings.tsx:
       - Remove `onOpenHowToPlay` from SettingsProps interface
       - Remove `onOpenHowToPlay` from component destructuring
       - Remove the "How to Play" button section (lines 148-153)

    2. In src/App.tsx:
       - Remove `onOpenHowToPlay` prop from Settings component (line 586)

    3. In src/App.css:
       - Remove the `.settings-h2p` and `.settings-h2p:active` CSS rules (lines 437-448)
  </action>
  <verify>
    npm run build — clean build, no references to onOpenHowToPlay in Settings component
  </verify>
  <done>
    Settings panel no longer has a How to Play button. The main menu How to Play button (added in Phase 22) is the sole access point. Build passes clean.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] No TypeScript errors or warnings
- [ ] grep confirms no remaining references to useShakeToRoll, useAccelerometerGravity, GravityController, shakeToRollEnabled, or shakeSupported
- [ ] grep confirms no onOpenHowToPlay in Settings.tsx
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Three files deleted (useShakeToRoll.ts, useAccelerometerGravity.ts, GravityController.tsx)
- Settings panel shows: Audio, Performance, Tips, Confirmation, divider, Main Menu/Privacy
- No shake-related code anywhere in codebase
  </success_criteria>

<output>
After completion, create `.planning/phases/23-settings-controls-cleanup/23-01-SUMMARY.md`
</output>
