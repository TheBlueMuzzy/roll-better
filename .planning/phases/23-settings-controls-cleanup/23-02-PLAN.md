---
phase: 23-settings-controls-cleanup
plan: 02
type: execute
---

<objective>
Replace the Settings text link on the main menu with a gear icon and fix the audio slider to show a visible filled track.

Purpose: Consistent settings access (gear icon everywhere), and the audio slider currently has no visual fill indicator making it hard to read.
Output: Gear icon on main menu, polished audio slider with filled track.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/phase-prompt.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-settings-controls-cleanup/23-01-SUMMARY.md

# Key files:
@src/components/MainMenu.tsx
@src/App.css
@src/components/Settings.tsx

**Constraining decisions:**
- Phase 22: menu-links wrapper groups How to Play + Upgrades + Settings
- Phase 22: menu-link-btn class for secondary menu actions
- HUD gear icon style: `.settings-gear` in App.css (existing pattern to match)

**Established patterns:**
- menu-link-btn: text-style button for secondary menu actions
- settings-gear: gear icon button positioned bottom-right
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Settings text link with gear icon on MainMenu</name>
  <files>src/components/MainMenu.tsx, src/App.css</files>
  <action>
    1. In src/components/MainMenu.tsx:
       - Remove the Settings button from the .menu-links div (the `menu-settings-link` button, line 80-82)
       - Add a gear icon button OUTSIDE the menu-links div, positioned bottom-right of the menu screen. Use the same gear Unicode character as HUD (&#x2699;) and a new class `menu-gear` that mirrors the `.settings-gear` positioning pattern.

    2. In src/App.css:
       - Add `.menu-gear` class: position absolute, bottom-right of the `.menu-backdrop`, same size and style as `.settings-gear` but scoped to the menu screen. Match the existing gear's font-size, padding, opacity, and active state.
       - Remove `.menu-settings-link` styles if they exist (check first).
  </action>
  <verify>
    npm run build — clean build. MainMenu no longer has Settings text in menu-links. Gear icon visible in bottom-right.
  </verify>
  <done>
    Main menu shows gear icon in bottom-right corner (matching HUD pattern). menu-links only contains How to Play and Upgrades.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix audio slider filled-track styling</name>
  <files>src/App.css, src/components/Settings.tsx</files>
  <action>
    1. In src/App.css, update `.settings-slider` styles to show a filled track:
       - Use a CSS linear-gradient background trick on the slider itself. Since pure CSS can't style the filled portion cross-browser without JS, use an inline style approach instead:

    2. In src/components/Settings.tsx:
       - Add an inline style on the range input that sets a linear-gradient background showing the filled portion. The gradient goes from a bright color (e.g. rgba(255,255,255,0.7)) for the filled part to the existing dim track color (rgba(255,255,255,0.2)) for the unfilled part.
       - Calculate the fill percentage: `audioVolume` is already 0-100, so use it directly as the gradient stop percentage.
       - Pattern: `style={{ background: \`linear-gradient(to right, rgba(255,255,255,0.7) ${audioVolume}%, rgba(255,255,255,0.2) ${audioVolume}%)\` }}`

    3. In src/App.css:
       - Ensure `.settings-slider` has `border-radius` on the track for polish
       - Add `height` to the slider itself (not just thumb) so the gradient background is visible: set height to match the track height `clamp(4px, 0.5vw, 6px)` and add `border-radius: 2px`
  </action>
  <verify>
    npm run build — clean build. Slider visually shows filled vs unfilled portions.
  </verify>
  <done>
    Audio slider shows a bright filled track from 0 to the current value, and a dim track for the remainder. Visually clear at any volume level. Touch-friendly on mobile.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All Phase 23 changes: shake-to-roll removed, Settings cleaned up, gear icon on main menu, audio slider fixed</what-built>
  <how-to-verify>
    1. Run: npm run dev (or dev server already running)
    2. Open in browser
    3. Main Menu:
       - Confirm NO "Settings" text link in the bottom links area
       - Confirm gear icon visible in bottom-right corner
       - Tap gear — Settings panel opens
       - Confirm How to Play and Upgrades still in menu-links
    4. Settings Panel (from menu):
       - Confirm NO "Shake to Roll" toggle
       - Confirm NO "How to Play" button
       - Confirm Audio slider shows filled track (bright left portion, dim right portion)
       - Drag slider — filled portion updates smoothly
       - Confirm Confirmation toggle still present
       - Confirm Performance, Tips toggles still present
    5. Start a game (tap PLAY)
       - Confirm status text says "Tap to Roll" (NOT "Shake or Tap to Roll")
       - Confirm gear icon in bottom-right opens Settings during game
       - Confirm NO "Enable Shake" button appears
    6. Settings Panel (from game):
       - Same checks as step 4
       - Confirm "Main Menu" quit button visible
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds without errors
- [ ] Main menu has gear icon (no Settings text link)
- [ ] Audio slider shows filled track visually
- [ ] All Phase 23 changes verified by user
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- User approved checkpoint
- Phase 23 complete
  </success_criteria>

<output>
After completion, create `.planning/phases/23-settings-controls-cleanup/23-02-SUMMARY.md`
</output>
