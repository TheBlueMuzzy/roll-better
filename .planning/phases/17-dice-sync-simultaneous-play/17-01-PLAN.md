---
phase: 17-dice-sync-simultaneous-play
plan: 01
type: execute
---

<objective>
Set up online game infrastructure: persistent socket access for game messages, online mode detection in the store, and a dedicated hook for server game action message routing.

Purpose: Foundation layer so subsequent plans can send/receive game messages without plumbing concerns.
Output: `useOnlineGame` hook wired into App.tsx, online mode flags in store, module-level socket accessible for game actions.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/phase-prompt.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context (direct dependencies):
@.planning/phases/16-state-sync-protocol/16-01-SUMMARY.md
@.planning/phases/16-state-sync-protocol/16-02-SUMMARY.md

# Key source files:
@src/utils/partyClient.ts
@src/hooks/useRoom.ts
@src/store/gameStore.ts
@src/types/protocol.ts
@src/types/game.ts
@src/App.tsx

**Tech stack available:** partykit, partysocket, zustand, react
**Established patterns:**
- Thin factory + helpers in partyClient.ts (createPartyConnection, sendMessage, parseServerMessage)
- useRoom hook manages socket lifecycle via useRef, sets socket.onmessage for lobby messages
- LobbyScreen uses `visible` prop — stays mounted during game, so useRoom hooks keep running and socket stays alive
- Protocol types in src/types/protocol.ts (shared client/server, no game.ts imports)
- Discriminated union message protocol (ClientMessage, ServerMessage)

**Constraining decisions:**
- [16-01]: Protocol types finalized — RollResultsMessage, PhaseChangeMessage, UnlockResultMessage ready for consumption
- [16-02]: Server drives phase transitions with timeouts (1s locking→unlocking, 20s AFK timeout)
- [15-02]: Online players marked isAI: true locally (player index 0 = local human, rest = remote/AI)
- [14-03]: partyClient.ts thin wrapper pattern, VITE_PARTY_HOST env config

**Issues being addressed:** ISS-004 (online rolls not synced — foundational wiring)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Module-level game socket + online mode store flags</name>
  <files>src/utils/partyClient.ts, src/store/gameStore.ts, src/hooks/useRoom.ts, src/App.tsx</files>
  <action>
  **partyClient.ts** — Add module-level game socket reference:
  ```
  let gameSocket: PartySocket | null = null;
  export function setGameSocket(s: PartySocket | null) { gameSocket = s; }
  export function getGameSocket(): PartySocket | null { return gameSocket; }
  ```
  This persists the socket reference outside React component lifecycle so useOnlineGame (Task 2) can access it independently of useRoom.

  **gameStore.ts** — Add online mode fields to GameState interface and initial state:
  - `isOnlineGame: boolean` (default false)
  - `onlinePlayerId: string | null` (default null) — the local player's server-assigned connection ID
  Add to GameStore interface:
  - `setOnlineMode: (playerId: string) => void` — sets isOnlineGame=true and onlinePlayerId
  - `clearOnlineMode: () => void` — resets both to defaults

  **useRoom.ts** — In the `game_starting` case handler, after setting gameStartData, call `setGameSocket(socketRef.current)` to store the socket for game-phase access. Import setGameSocket from partyClient.

  **App.tsx** — In `handleOnlineGameStart`, after calling initGame+initRound, call `useGameStore.getState().setOnlineMode(localPlayerId)` to mark this as an online game. In `handleMenu`, call `clearOnlineMode()` and `setGameSocket(null)` to clean up when returning to menu.

  Do NOT modify the useRoom cleanup effect — the socket stays alive because LobbyScreen stays mounted (visible prop pattern). Do NOT add gameSocket cleanup to useRoom — App.tsx owns the game lifecycle cleanup.
  </action>
  <verify>
  Build passes: `cd "C:/Users/Muzzy/Documents/UnityProjects/Roll Better" && npx tsc --noEmit`
  Verify: `getGameSocket` is importable, store has `isOnlineGame`/`onlinePlayerId` fields
  </verify>
  <done>
  - partyClient.ts exports setGameSocket/getGameSocket
  - gameStore has isOnlineGame (default false), onlinePlayerId (default null), setOnlineMode, clearOnlineMode
  - useRoom stores socket on game_starting
  - App.tsx sets online mode on game start, clears on menu return
  - TypeScript compiles clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Create useOnlineGame hook with message routing and action senders</name>
  <files>src/hooks/useOnlineGame.ts, src/App.tsx</files>
  <action>
  **Create src/hooks/useOnlineGame.ts:**

  Hook that manages game-phase server message handling. Uses addEventListener (not onmessage) so it coexists with useRoom's handler without conflict.

  ```typescript
  interface UseOnlineGameReturn {
    sendRollRequest: () => void;
    sendUnlockRequest: (slotIndices: number[]) => void;
    sendSkipUnlock: () => void;
  }
  ```

  Implementation:
  - Read `isOnlineGame` from useGameStore
  - If not online, return no-op functions (early return pattern)
  - useEffect: get socket via `getGameSocket()`, add message event listener, clean up on unmount
  - Message handler switch on `msg.type`:
    - `"roll_results"`: Store in `pendingServerResults` ref (will be consumed by 17-02's store action). For now, just console.log the results so we can verify message routing works. Store them via a new store action `setPendingServerResults(msg.playerResults)`.
    - `"phase_change"`: Call `useGameStore.getState().setPhase(msg.phase as GamePhase)`. But ONLY apply if different from current phase (avoid loops).
    - `"unlock_result"`: Console.log for now (wired in 17-03). Store via `setPendingUnlockResult(msg)`.
    - `"round_start"`, `"scoring"`, `"session_end"`: Console.log only (Phase 18 scope). Do NOT handle these yet.
  - Action senders use `sendMessage(socket, { type: "roll_request" })` etc. via getGameSocket(). Guard: if socket is null, warn and return.

  **gameStore.ts** — Add pending server data fields:
  - `pendingServerResults: PlayerRollResult[] | null` (default null)
  - `setPendingServerResults: (results: PlayerRollResult[] | null) => void`
  - `pendingUnlockResult: UnlockResultMessage | null` (default null)
  - `setPendingUnlockResult: (result: UnlockResultMessage | null) => void`
  Import PlayerRollResult and UnlockResultMessage from protocol.ts.

  **App.tsx** — Call `useOnlineGame()` at the top level (after existing hooks). Destructure `sendRollRequest` (used in 17-02), `sendUnlockRequest`, `sendSkipUnlock` (used in 17-03). For now these are just available — wiring into handleRoll/handleConfirmUnlock happens in subsequent plans.

  Do NOT use socket.onmessage (useRoom already sets that). Use addEventListener('message', handler) which fires alongside onmessage with no conflict. Do NOT handle scoring/round_start/session_end beyond console.log — that's Phase 18.
  </action>
  <verify>
  Build passes: `cd "C:/Users/Muzzy/Documents/UnityProjects/Roll Better" && npx tsc --noEmit`
  Start local game (offline) — verify no regressions, useOnlineGame returns no-op functions
  Start Partykit server (`npx partykit dev`), create online room, start game — verify console shows "roll_results", "phase_change" etc. when server sends them (won't happen yet since client doesn't send roll_request, but the listener is wired)
  </verify>
  <done>
  - useOnlineGame hook exists with message listener + 3 action senders
  - Store has pendingServerResults + pendingUnlockResult fields
  - Hook wired into App.tsx
  - Offline game works with zero regressions
  - TypeScript compiles clean
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes
- [ ] Offline local game works identically (no regressions)
- [ ] Online game starts from lobby (game_starting still works)
- [ ] Store shows isOnlineGame=true after online game starts
- [ ] getGameSocket() returns the socket during online game
- [ ] useOnlineGame hook is active and listening for messages
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- Offline game unaffected
- Online infrastructure ready for roll sync (17-02) and unlock sync (17-03)
</success_criteria>

<output>
After completion, create `.planning/phases/17-dice-sync-simultaneous-play/17-01-SUMMARY.md`
</output>
