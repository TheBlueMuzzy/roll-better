---
phase: 12-responsive-ui
plan: 03
type: execute
---

<objective>
Make Main Menu and Winners Screen fully responsive across all phone sizes.

Purpose: Both screens use max-width: 280-320px on key elements (play button, winners list) that waste space on larger devices. Apply the responsive custom properties established in 12-01.
Output: Menu and Winners screens that scale gracefully from iPhone SE (375px) to iPad (1024px).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/phase-prompt.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plans in this phase:
@.planning/phases/12-responsive-ui/12-01-SUMMARY.md
@.planning/phases/12-responsive-ui/12-02-SUMMARY.md

# Directly relevant prior work:
@.planning/phases/10-screens-flow/10-03-SUMMARY.md

# Key source files:
@src/App.css
@src/components/MainMenu.tsx
@src/components/WinnersScreen.tsx

**CSS custom properties available (from 12-01):**
- --fs-xs, --fs-sm, --fs-md, --fs-lg, --fs-xl (font sizes)
- --sp-xs, --sp-sm, --sp-md, --sp-lg, --sp-xl (spacing)
- --overlay-max-w, --btn-max-w, --touch-target, --border-radius (layout)

**Constraining decisions:**
- Phase 10: Menu at z-70, Winners at z-70
- CSS opacity transitions (0.3s) for screen fades
- pointer-events: none on hidden overlays
- rAF-class-toggle for entrance animations
- Game preferences stored in Zustand (session-scoped)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Main Menu responsive</name>
  <files>src/App.css</files>
  <action>
Replace hardcoded values in Main Menu CSS classes with custom properties:

**`.menu-content`:**
- gap → var(--sp-lg)
- padding → var(--sp-lg)

**`.menu-title`:**
- font-size: 2.5rem is already relative — keep as-is (good pattern)
- margin-bottom → var(--sp-xs)

**`.menu-subtitle`:**
- font-size: 0.9rem — keep as-is (already relative)
- margin-bottom → var(--sp-lg)

**`.menu-section`:**
- gap → var(--sp-sm)

**`.menu-selector`:**
- gap: 8px → var(--sp-xs)

**`.menu-btn`:**
- padding: 12px 22px → var(--sp-sm) var(--sp-lg)
- font-size: 16px → var(--fs-md)
- border-radius: 8px → var(--border-radius)

**`.menu-play`:**
- max-width: 280px → var(--btn-max-w)
- padding: 16px → var(--sp-md)
- font-size: 18px → var(--fs-lg)
- border-radius: 12px → var(--border-radius)

**`.menu-gear`:**
- bottom: 24px → var(--sp-xl)
- right: 14px → var(--sp-sm)
- width/height → var(--touch-target)
- font-size: 24px → var(--fs-xl)

**`.menu-label`:**
- font-size: 14px → var(--fs-sm)

Do NOT change z-index values. Do NOT modify MainMenu.tsx (only CSS changes).
  </action>
  <verify>Run `npm run build`. Open Main Menu in DevTools at 375px and 768px — play button should be wider on large screens, selector buttons should have comfortable spacing on small screens. Gear button should remain tappable.</verify>
  <done>Main Menu uses responsive custom properties. Play button and selectors scale with viewport. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Winners Screen responsive</name>
  <files>src/App.css</files>
  <action>
Replace hardcoded values in Winners Screen CSS classes with custom properties:

**`.winners-content`:**
- padding → var(--sp-lg)

**`.winners-heading`:**
- font-size: 2rem — keep as-is (already relative)

**`.winners-announcement`:**
- font-size: 1.4rem — keep as-is (already relative)
- margin-bottom → var(--sp-md)

**`.winners-list`:**
- max-width: 320px → var(--overlay-max-w)
- gap → var(--sp-sm)

**`.winners-row`:**
- padding: 10px 14px → var(--sp-sm) var(--sp-md)
- gap: 12px → var(--sp-sm)
- border-radius: 12px → var(--border-radius)

**`.winners-avatar`:**
- width/height: 36px → clamp(32px, 8vw, 48px)
- font-size: 16px → var(--fs-md)

**`.winners-name`:**
- font-size: 15px → var(--fs-md)

**`.winners-score`:**
- font-size: 20px → var(--fs-xl)

**`.winners-rank`:**
- font-size: 14px → var(--fs-sm)

**`.winners-play-again`:**
- max-width: 280px → var(--btn-max-w)
- padding: 16px → var(--sp-md)
- font-size: 18px → var(--fs-lg)
- border-radius: 12px → var(--border-radius)

**`.winners-stats` (if exists):**
- font-size → var(--fs-xs)
- gap → var(--sp-xs)

Do NOT change z-index values. Do NOT modify WinnersScreen.tsx (only CSS changes).
  </action>
  <verify>Run `npm run build`. Open Winners Screen in DevTools at 375px and 768px — winners list should use more width on large screens, avatars should scale, play again button should be wider. Scores should remain legible on small screens.</verify>
  <done>Winners Screen uses responsive custom properties. List and button widths scale via clamp(). Avatar and text sizes scale. Build passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Main Menu scales from 375px to 768px+ without content cut-off
- [ ] Menu selector buttons have comfortable spacing at all sizes
- [ ] Winners Screen scales from 375px to 768px+ without content cut-off
- [ ] Winners list rows don't feel cramped on iPhone SE
- [ ] No visual regressions at default viewport (390px iPhone 14)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No hardcoded px values remain in Menu or Winners CSS (except rem-based titles)
- Both screens visually adapt to viewport size
</success_criteria>

<output>
After completion, create `.planning/phases/12-responsive-ui/12-03-SUMMARY.md`
</output>
