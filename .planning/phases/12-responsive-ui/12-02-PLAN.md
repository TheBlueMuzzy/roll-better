---
phase: 12-responsive-ui
plan: 02
type: execute
---

<objective>
Make Settings panel and How to Play carousel fully responsive across all phone sizes.

Purpose: These overlays currently have fixed max-widths (320px, 340px) that waste space on tablets and constrain content on large phones. Replace with scalable constraints.
Output: Settings and H2P overlays that scale gracefully from iPhone SE (375px) to iPad (1024px) with no cut-off content.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/phase-prompt.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan in this phase:
@.planning/phases/12-responsive-ui/12-01-SUMMARY.md

# Directly relevant prior work:
@.planning/phases/07-unlock-interaction/07-01-SUMMARY.md

# Key source files:
@src/App.css
@src/components/Settings.tsx
@src/components/HowToPlay.tsx

**CSS custom properties available (from 12-01):**
- --fs-xs, --fs-sm, --fs-md, --fs-lg, --fs-xl (font sizes)
- --sp-xs, --sp-sm, --sp-md, --sp-lg, --sp-xl (spacing)
- --overlay-max-w, --btn-max-w, --touch-target, --border-radius (layout)

**Constraining decisions:**
- Phase 7: Settings at z-50, H2P at z-60
- Settings panel uses HTML overlay with backdrop dismiss
- H2P uses touch swipe via refs, breadcrumb dots
- Toggle-group pattern: toggle + hint label centered underneath
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings panel responsive</name>
  <files>src/App.css</files>
  <action>
Replace hardcoded values in Settings CSS classes with custom properties:

**`.settings-panel`:**
- max-width: 320px → var(--overlay-max-w) (clamps 280px–420px)
- padding: 24px → var(--sp-lg)
- border-radius: 16px → var(--border-radius)

**`.settings-title`:**
- font-size: 18px → var(--fs-lg)

**`.settings-item` / `.settings-label`:**
- font-size: 14px → var(--fs-sm)
- gap values → var(--sp-sm)

**`.settings-hint`:**
- font-size: 11px → var(--fs-xs)

**`.settings-toggle`:**
- width: 44px → var(--touch-target)
- height: 24px → calc(var(--touch-target) * 0.55)
- `.settings-toggle-thumb` width/height: 16px → calc(var(--touch-target) * 0.36)
- Adjust translateX for thumb ON position to match new width

**`.settings-slider`:**
- height: 4px → clamp(4px, 0.5vw, 6px)
- `.settings-slider-thumb` width/height → calc(var(--touch-target) * 0.45)

**`.settings-quit`:**
- padding: 14px → var(--sp-md)
- font-size: 16px → var(--fs-md)
- border-radius: 8px → var(--border-radius)

**`.settings-quit-confirm` buttons:**
- padding → var(--sp-sm) var(--sp-md)
- font-size → var(--fs-sm)
- gap → var(--sp-sm)

Do NOT change z-index values. Do NOT modify Settings.tsx (only CSS changes).
  </action>
  <verify>Run `npm run build`. Open Settings panel in DevTools at 375px and 768px — panel should fill more width on small screens, not look tiny on large screens. Toggle buttons should remain tappable. Slider thumb should be easy to grab.</verify>
  <done>Settings panel uses responsive custom properties. max-width scales via clamp(). Toggles and slider scale with viewport. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: How to Play carousel responsive</name>
  <files>src/App.css</files>
  <action>
Replace hardcoded values in H2P CSS classes with custom properties:

**`.h2p-card`:**
- max-width: 340px → var(--overlay-max-w)
- border-radius: 16px → var(--border-radius)

**`.h2p-slide`:**
- padding: 32px 24px → var(--sp-xl) var(--sp-lg)

**`.h2p-visual`:**
- height: 160px → clamp(120px, 30vw, 200px)

**`.h2p-title`:**
- font-size: 20px → var(--fs-xl)

**`.h2p-text`:**
- font-size: 14px → var(--fs-sm)
- line-height: 1.5 (keep as-is, unitless is correct)

**`.h2p-icon`:**
- font-size: 64px → clamp(48px, 12vw, 80px)

**`.h2p-close`:**
- width/height → var(--touch-target)
- font-size: 24px → var(--fs-xl)

**`.h2p-arrow`:**
- width/height: 44px → var(--touch-target)
- font-size: 28px → var(--fs-xl)

**`.h2p-dots` / `.h2p-dot`:**
- width/height: 10px → clamp(8px, 2vw, 12px)
- padding: 12px → var(--sp-sm)
- gap → var(--sp-xs)

Do NOT change z-index values. Do NOT modify HowToPlay.tsx (only CSS changes).
  </action>
  <verify>Run `npm run build`. Open H2P carousel in DevTools at 375px and 768px — card should be wider on large screens, slides should not feel cramped on small screens. Navigation arrows and dots should be tappable. Icons should scale proportionally.</verify>
  <done>H2P carousel uses responsive custom properties. Card width scales via clamp(). Slide content scales with viewport. Build passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Settings panel scales from 375px to 768px+ without content cut-off
- [ ] Settings toggles and slider remain usable touch targets at all sizes
- [ ] H2P carousel scales from 375px to 768px+ without content cut-off
- [ ] H2P swipe navigation still works (touch handlers untouched)
- [ ] No visual regressions at default viewport (390px iPhone 14)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No hardcoded px values remain in Settings or H2P CSS
- Both overlays visually adapt to viewport size
</success_criteria>

<output>
After completion, create `.planning/phases/12-responsive-ui/12-02-SUMMARY.md`
</output>
