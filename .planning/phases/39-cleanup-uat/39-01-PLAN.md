---
phase: 39-cleanup-uat
plan: 01
type: execute
---

<objective>
Final cleanup and UAT for v1.4 Landscape milestone.

Purpose: Verify the entire landscape conversion works end-to-end. Check for any remaining portrait artifacts, ensure all screens and flows work correctly in 16:9.
Output: Clean build, all flows verified, v1.4 ready to ship.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/PlayerIcon.tsx

**Milestone summary:**
- Phase 35: 16:9 viewport, vh-based tokens, landscape PWA ✓
- Phase 36: Left/right split 3D layout, rows left, rolling right ✓
- Phase 37: HUD repositioned for landscape ✓
- Phase 37.1: Profile elements converted to 3D ✓
- Phase 38: Menu/modal screens responsive ✓

**Code audit results (from planning):**
- No portrait-specific code found
- No dead code or unused imports
- PlayerIcon.tsx still uses Html overlay (used for in-game labels — separate from profile groups)
- All CSS tokens already landscape-appropriate
- FOV hardcoded at 55° (no branching)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Check PlayerIcon.tsx and run final build</name>
  <files>src/components/PlayerIcon.tsx</files>
  <action>
1. **Read PlayerIcon.tsx** — check if it's still used. It may be a leftover from before PlayerProfileGroup existed. If it's imported in Scene.tsx or App.tsx, it's still needed. If not imported anywhere, it can be left (don't delete — out of scope for cleanup, and it might be used for online player labels).

2. **Check PlayerIcon responsive scale** — it uses `window.innerWidth / 390` baseline. This is fine for landscape (just adapts to screen width). No change needed unless it causes issues.

3. **Run full build and type check:**
   - `npm run build` — must pass with no errors
   - `npx tsc --noEmit` — must pass with no type errors

4. **Grep for any remaining hardcoded portrait values:**
   - Search for `9:16`, `portrait`, `innerWidth < innerHeight` across src/
   - Should find nothing (confirmed in planning audit)
  </action>
  <verify>npm run build passes. npx tsc --noEmit passes. No portrait-specific code found.</verify>
  <done>Clean build, no type errors, no portrait artifacts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>v1.4 Landscape milestone — complete landscape conversion of Roll Better</what-built>
  <how-to-verify>
    **Full UAT walkthrough — test every screen and flow:**

    1. Run: npm run dev -- --host
    2. Open on phone in landscape AND in browser

    **Main Menu:**
    3. Title "Roll Better" visible, proportional
    4. PLAY LOCAL button tappable, reasonable width
    5. CREATE/JOIN buttons visible
    6. ? and gear buttons at bottom corners
    7. Click gear → Settings opens, readable, closeable
    8. Click ? → HowToPlay opens, swipeable, closeable

    **Local Game (phone size 844x390):**
    9. Start local game — 3D scene renders in left/right split
    10. Goal row at top-left with star icon
    11. Player rows below with colored avatar circles + stars + stats
    12. Dice in rolling area (right half)
    13. "Tap to Roll" over rolling area
    14. Roll dice — they spawn, tumble, settle
    15. Lock phase — dice fly to goal row slots
    16. Unlock phase — SKIP/UNLOCK button in rolling area
    17. AI turns animate correctly
    18. Round transitions work
    19. Play to session end — Winners screen appears, readable

    **Wide Browser (~1440x810):**
    20. Repeat steps 9-19 — verify scaling
    21. No overlap between profiles and dice at wide size
    22. Everything proportional

    **Online (if available):**
    23. Click CREATE — room code appears
    24. Code input for JOIN visible
    25. Seat notifications appear (if testing with second device)

    **Known acceptable issues (not blockers):**
    - Bot icon shows "B" instead of robot emoji (art pass will fix)
    - Star shapes are placeholder geometry (art pass will replace)
    - Score numbers on stars may not render (font loading)
    - drei/Text default font limitations
  </how-to-verify>
  <resume-signal>Type "approved" to ship v1.4, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring milestone complete:
- [ ] `npm run build` succeeds
- [ ] `npx tsc --noEmit` passes
- [ ] No portrait-specific code remains
- [ ] All screens render in landscape
- [ ] Game playable end-to-end in landscape
- [ ] No visual regressions from v1.3
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- v1.4 Landscape milestone complete
- Ready to merge and deploy
</success_criteria>

<output>
After completion, create `.planning/phases/39-cleanup-uat/39-01-SUMMARY.md`
</output>
