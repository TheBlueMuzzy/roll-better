---
phase: 31-host-migration-room-lifecycle
plan: 02
type: execute
---

<objective>
Add Room Full UI with TRY AGAIN button and handle room dissolution for in-game clients.

Purpose: Currently room-full errors show a generic message with no retry. And when the server dissolves a room (all-bots from 31-01), in-game clients need to gracefully return to the main menu.
Output: TRY AGAIN button on room full, graceful menu return on room dissolution.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-host-migration-room-lifecycle/31-01-SUMMARY.md

# Key source files:
@src/hooks/useRoom.ts
@src/components/MainMenu.tsx
@src/hooks/useOnlineGame.ts
@src/types/protocol.ts
@src/App.css

**Established patterns:**
- useRoom has onlineMode state machine: 'idle' | 'creating' | 'joining' | 'joined'
- Error state in useRoom: joinError string displayed in MainMenu
- room_closed already handled in useRoom for reconnect-after-grace (29-01)
- useOnlineGame handles in-game protocol messages
- MainMenu shows error text below join input

**From 31-01:** dissolveRoom() broadcasts room_closed with reason string to all connections + mid-game joiners.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Room Full error with TRY AGAIN button</name>
  <files>src/hooks/useRoom.ts, src/components/MainMenu.tsx, src/App.css</files>
  <action>
1. In useRoom.ts, check how the current "Room is full" error is received. The server sends `{ type: "error", message: "Room is full (max 8 players)" }`. This likely sets joinError state. Confirm the error handler and how joinError is consumed.

2. In MainMenu.tsx, find where joinError is displayed (likely near the join input). Add a TRY AGAIN button that:
   - Clears the error state
   - Re-attempts the join with the same room code
   - Only shows when the error contains "full" (or use a distinct error code)
   - Style: same as existing buttons in the menu, positioned below the error text

3. Optionally, improve the error to be more specific on the server side:
   - In party/server.ts, change the room-full error to include a `code` field: `{ type: "error", code: "room_full", message: "Room is full" }`
   - In useRoom.ts, detect `code === "room_full"` to show the TRY AGAIN button (more robust than string matching)

4. The TRY AGAIN button should call the same join function that the initial Join button calls. It just re-sends the join request with the existing room code. The user stays on the join screen — they don't go back to idle mode.

**What to avoid:** Don't auto-retry — the user should tap the button. Don't add a retry timer or polling. Keep it simple: error message + one button.
  </action>
  <verify>
1. `npx tsc --noEmit` passes
2. Read MainMenu.tsx and confirm TRY AGAIN button renders conditionally on room_full error
3. Read useRoom.ts and confirm error handling distinguishes room_full
  </verify>
  <done>Room Full error shows "Room is Full" with a TRY AGAIN button. Tapping retries the join. No auto-retry.</done>
</task>

<task type="auto">
  <name>Task 2: Room dissolved — in-game clients return to menu</name>
  <files>src/hooks/useOnlineGame.ts, src/hooks/useRoom.ts, src/components/MainMenu.tsx</files>
  <action>
1. Check existing room_closed handling in useRoom.ts. This was added in Phase 29 for reconnect-after-grace. It likely resets the room state and shows an error or navigates back to menu.

2. For in-game clients (actively playing when dissolution happens):
   - useOnlineGame.ts needs to handle the `room_closed` message during gameplay
   - On receiving room_closed: call the game cleanup/exit function (likely the same path as "return to menu" from winners screen or the existing bot-takeover menu return from 28-02)
   - Show a brief notification: "Room closed — all players left" (or similar based on the reason field)

3. For mid-game joiners (on seat selection screen when dissolution happens):
   - useRoom.ts already handles room_closed — verify it clears the mid-game join state (seatList, claimedSeat) and shows appropriate feedback
   - Should reset to idle mode with an error message like "Room closed"

4. For pre-game/lobby clients:
   - useRoom.ts room_closed handler should reset to idle mode
   - Show "Room closed" as joinError

**What to avoid:** Don't add complex reconnection logic — room_closed means the room is gone. Just clean up and return to menu. Don't show a modal — just reset state and show error text inline like other errors.
  </action>
  <verify>
1. `npx tsc --noEmit` passes
2. Grep for room_closed handling in useOnlineGame.ts — should have a handler
3. Read useRoom.ts room_closed handler and confirm it covers mid-game join state cleanup
4. `npm run build` succeeds
  </verify>
  <done>In-game clients gracefully return to menu on room dissolution. Mid-game joiners and lobby clients see "Room closed" error. All room state properly cleaned up.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Room Full error shows TRY AGAIN button in MainMenu
- [ ] TRY AGAIN retries join with same room code
- [ ] room_closed during gameplay returns player to menu
- [ ] room_closed during mid-game join clears seat selection state
- [ ] room_closed during lobby shows error text
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors or build warnings
- Room Full has actionable UI (not just error text)
- Room dissolution handled gracefully in all client states
- Phase 31 complete
</success_criteria>

<output>
After completion, create `.planning/phases/31-host-migration-room-lifecycle/31-02-SUMMARY.md`
</output>
