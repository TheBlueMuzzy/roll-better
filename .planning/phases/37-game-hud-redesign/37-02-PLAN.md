---
phase: 37-game-hud-redesign
plan: 02
type: execute
---

<objective>
Reposition tip banner and unlock/skip button for landscape layout.

Purpose: The tip banner (`top: 36%`) and unlock button (`top: 55%, left: 50%`) use portrait-era absolute positioning that doesn't work with the left/right split layout. Both need to appear over the rolling area (right half) where the player's attention is.
Output: Tip banner and unlock button correctly positioned for landscape, completing the HUD redesign.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-game-hud-redesign/37-01-SUMMARY.md

@src/components/TipBanner.tsx
@src/components/HUD.tsx
@src/App.css

**Tech stack available:** React 18, CSS custom properties, vh/vw-based responsive tokens
**Established patterns:**
- Left/right split: rows on left (X ~ -4), rolling on right (X ~ 5)
- Status text anchored to right half (from 37-01)
- vh-based scaling tokens

**Constraining decisions:**
- Phase 36: Left half = rows, Right half = rolling area
- Plan 37-01: Status text moved to right half, touch targets use vmin
- Muzzy: positions are usability-first, art pass may adjust later
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reposition tip banner for landscape</name>
  <files>src/App.css</files>
  <action>
The tip banner currently uses `top: 36%` which places it mid-viewport in portrait. In landscape with the left/right split, it should appear over the rolling area (right half).

**Update `.tip-banner`:**
- Change `top: 36%` to `top: var(--sp-lg)` — position near top of viewport
- Change `left: var(--sp-md)` to `left: 52%` — start just past the center divider
- Change `right: var(--sp-md)` to `right: var(--sp-lg)` — pad from right edge
- This places the tip banner as a horizontal strip across the top of the rolling area

The tip should be unobtrusive but visible. The slide-down animation (`translateY(-20px)` → `translateY(0)`) still works for a top-positioned banner.
  </action>
  <verify>npm run build passes. Inspect `.tip-banner` positioning in DevTools at 844x390 — should span from ~52% left to right edge, near top.</verify>
  <done>Tip banner appears over the rolling area half, not spanning full viewport width.</done>
</task>

<task type="auto">
  <name>Task 2: Reposition unlock/skip button for landscape</name>
  <files>src/App.css</files>
  <action>
The unlock/skip button uses `top: 55%, left: 50%` which centers it in the full viewport — in landscape this lands on the divider between rows and rolling area.

**Update `.hud-skip-btn`:**
- Change `left: 50%` to `left: 75%` — center it in the right half (rolling area)
- Keep `top: 55%` — vertical center of rolling area is roughly correct
- Keep `transform: translate(-50%, -50%)` — centering logic still works

This places the unlock/skip button in the middle of the rolling area where the dice are, which is where the player's attention already is.

**Touch target:** The button uses `padding: var(--sp-md) var(--sp-xl)` which should be adequate with the fixed tokens from 37-01. Verify the button is at least 44px tall in landscape.
  </action>
  <verify>npm run build passes. Start a game, get to unlock phase — button should appear centered in the right half (rolling area), not straddling the divider.</verify>
  <done>Unlock/skip button appears over rolling area, finger-tappable, visually centered in right half.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Tip banner and unlock button repositioned for landscape — both over the rolling area (right half)</what-built>
  <how-to-verify>
    1. Run: npm run dev -- --host
    2. Open on phone in landscape or DevTools at 844x390
    3. Start a local game (vs AI)
    4. First game: tip banner should appear over the rolling area (right half), near the top
    5. Dismiss tip — tap or wait for auto-dismiss
    6. Roll dice, let them lock
    7. Unlock phase: verify UNLOCK/SKIP button appears centered in the rolling area (right half)
    8. Verify button is large enough to tap comfortably
    9. Tap to unlock/skip — verify it works
    10. Check: No visual overlap between button and 3D dice rows
    11. Check: No overlap between tip banner and round counter
    12. Note: Menu/screens redesign is Phase 38
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 37, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds without errors
- [ ] Tip banner appears over rolling area
- [ ] Unlock button centered in right half
- [ ] Both elements tappable with landscape touch targets
- [ ] No overlap with 3D scene elements
- [ ] All HUD elements work correctly in landscape
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Phase 37 complete — all HUD elements positioned for landscape
- Ready for Phase 38 (Menu & Screens)
</success_criteria>

<output>
After completion, create `.planning/phases/37-game-hud-redesign/37-02-SUMMARY.md`
</output>
