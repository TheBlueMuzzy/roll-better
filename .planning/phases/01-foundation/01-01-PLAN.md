---
phase: 01-foundation
plan: 01
type: execute
---

<objective>
Scaffold the Roll Better project with Vite + React + TypeScript + R3F + Rapier + Zustand + drei.

Purpose: Establish the dev environment and project structure that all future phases build on.
Output: A running Vite dev server with R3F Canvas rendering, Zustand store initialized, all dependencies installed.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Tech stack (from PROJECT.md constraints):**
- React 18+ / TypeScript / Vite
- three / @react-three/fiber / @react-three/drei / @react-three/rapier
- Zustand (state management)
- Howler.js (audio — not needed yet, Phase 12)

**Key constraint:** R3F Golden Rule — never use React state for per-frame updates, mutate refs in useFrame.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vite project and install dependencies</name>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, index.html</files>
  <action>
  1. Run `npm create vite@latest . -- --template react-ts` in the project root to scaffold Vite + React + TypeScript.
  2. Run `npm install` to install base dependencies.
  3. Install R3F ecosystem: `npm install three @react-three/fiber @react-three/drei @react-three/rapier`
  4. Install Zustand: `npm install zustand`
  5. Install Three.js types: `npm install -D @types/three`
  6. Clean up Vite boilerplate: remove `src/App.css` content (keep file), remove counter logic from `App.tsx`, remove `public/vite.svg` and `src/assets/react.svg`.
  7. Update `index.html` title to "Roll Better".

  Do NOT install Howler.js yet (Phase 12). Do NOT add eslint plugins beyond what Vite scaffolds.
  </action>
  <verify>
  Run `npm run dev` — dev server starts on localhost without errors.
  Run `npx tsc --noEmit` — no TypeScript errors.
  </verify>
  <done>Vite dev server starts, all dependencies in package.json, TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Set up project structure and Zustand store skeleton</name>
  <files>src/App.tsx, src/App.css, src/store/gameStore.ts, src/types/game.ts, src/components/.gitkeep, src/components/Scene.tsx</files>
  <action>
  1. Create folder structure:
     - `src/components/` — React + R3F components
     - `src/store/` — Zustand stores
     - `src/types/` — TypeScript type definitions

  2. Create `src/types/game.ts` with skeleton types:
     - `GamePhase` enum: 'lobby' | 'rolling' | 'locking' | 'unlocking' | 'scoring' | 'roundEnd' | 'sessionEnd'
     - `Player` interface: id, name, color, score, handicap, dice (array), isAI
     - `Die` interface: id, value (1-6 | null), isLocked, position
     - `GameState` interface: phase, players, goalDice, currentRound, currentPlayerIndex

  3. Create `src/store/gameStore.ts` with Zustand store:
     - Import `create` from zustand
     - Define store with GameState + basic actions (reset, setPhase)
     - Initial state: phase 'lobby', empty players array, empty goalDice, round 1
     - This is a skeleton — game logic fills in during Phase 5

  4. Create `src/components/Scene.tsx`:
     - A simple R3F component that returns a group with a placeholder comment
     - This is where Phase 01-02 will build the 3D scene

  5. Update `src/App.tsx`:
     - Import Canvas from @react-three/fiber
     - Render Canvas component filling the viewport (100vw x 100vh)
     - Render Scene inside Canvas
     - Canvas props: camera={{ position: [0, 5, 8], fov: 50 }}

  6. Update `src/App.css`:
     - Reset: margin 0, padding 0, overflow hidden
     - html/body/#root: width 100vw, height 100vh (use dvh for mobile if supported)
     - Canvas fills container

  Do NOT add game logic to the store — just the type skeleton and empty actions.
  Do NOT add OrbitControls or lighting yet — that's Plan 01-02.
  </action>
  <verify>
  Run `npm run dev` — blank Canvas renders (black screen, no errors).
  Run `npx tsc --noEmit` — no TypeScript errors.
  Browser console has no errors or warnings.
  </verify>
  <done>Folder structure exists. Zustand store initializes. Canvas renders full-viewport. Types compile. No console errors.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` starts without errors
- [ ] `npx tsc --noEmit` passes
- [ ] Browser shows full-viewport Canvas (black screen is fine)
- [ ] `src/store/gameStore.ts` exports usable store
- [ ] `src/types/game.ts` exports GamePhase, Player, Die, GameState types
- [ ] No console errors or warnings in browser
</verification>

<success_criteria>

- All dependencies installed (three, @react-three/fiber, @react-three/drei, @react-three/rapier, zustand)
- Project compiles and runs
- Zustand store skeleton functional
- Type definitions in place for game concepts
- Canvas renders in browser
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
