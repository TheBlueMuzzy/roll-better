---
phase: 38-menu-screens
plan: 02
type: execute
---

<objective>
Widen Settings, HowToPlay, and WinnersScreen modals for landscape 16:9.

Purpose: All modal overlays use --overlay-max-w (now wider from 38-01) but some have hardcoded font sizes and spacing that don't adapt to landscape. The WinnersScreen and HTP carousel need responsive sizing.
Output: All overlay screens look proportional and usable in landscape.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-menu-screens/38-01-SUMMARY.md

@src/components/Settings.tsx
@src/components/HowToPlay.tsx
@src/components/WinnersScreen.tsx
@src/App.css

**Tech stack available:** React 18, CSS custom properties
**Established patterns from 38-01:**
- Wider --overlay-max-w and --btn-max-w tokens
- Responsive font sizing via clamp() and CSS tokens

**Current modal issues:**
- .settings-panel: max-width var(--overlay-max-w) — already widened by 38-01 token change, should be OK
- .h2p-card: uses --overlay-max-w — same, should benefit from 38-01
- .h2p-visual: height clamp(120px, 30vw, 200px) — 30vw could be large in landscape, may need adjustment
- .winners-heading: 2rem fixed — needs responsive token
- .winners-announcement: 1.4rem fixed — needs responsive token
- .winners-list: max-width var(--overlay-max-w) — benefits from 38-01
- .winners-row stagger animation: should work fine
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Settings and HowToPlay modal CSS for landscape</name>
  <files>src/App.css</files>
  <action>
**Settings panel:**
- .settings-panel already uses --overlay-max-w which was widened in 38-01
- Change `max-height: 80vh` to `max-height: 85vh` — in landscape vh is short, allow more height
- Verify settings-header margin-bottom uses responsive token: change `margin-bottom: 20px` to `margin-bottom: var(--sp-md)`

**HowToPlay card:**
- .h2p-card already uses --overlay-max-w — benefits from 38-01 token change
- .h2p-visual: change `height: clamp(120px, 30vw, 200px)` to `height: clamp(80px, 20vh, 180px)` — scale with height (short axis in landscape) not width, and smaller max for landscape
- .h2p-title: verify uses responsive token (should already use --fs-lg or similar)
- .h2p-text: verify uses responsive token

**No structural changes needed** — both modals are centered overlays that work in any aspect ratio. Just sizing adjustments.
  </action>
  <verify>npm run build passes. Settings panel and HTP carousel render at reasonable sizes in 844x390 landscape viewport.</verify>
  <done>Settings and HTP modals sized for landscape. No overflow, readable text.</done>
</task>

<task type="auto">
  <name>Task 2: Update WinnersScreen CSS for landscape</name>
  <files>src/App.css</files>
  <action>
**Winners backdrop:**
- Change `gap: 16px` to `gap: var(--sp-sm)` — use responsive token instead of fixed pixels

**Winners heading:**
- Already updated in 38-01 (if done) or update here: change `font-size: 2rem` to `font-size: clamp(1.4rem, 5vh, 2.2rem)`

**Winners announcement:**
- Change `font-size: 1.4rem` to `font-size: clamp(1rem, 3.5vh, 1.6rem)` — responsive

**Winners list:**
- Already uses --overlay-max-w — benefits from 38-01 widening

**Winners actions:**
- Verify buttons use responsive tokens for padding and font size
  </action>
  <verify>npm run build passes. Winners screen renders at reasonable size in landscape viewport.</verify>
  <done>WinnersScreen responsive for landscape. Heading and announcement text scale with viewport.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All overlay screens (Settings, HowToPlay, WinnersScreen) adjusted for landscape</what-built>
  <how-to-verify>
    1. Run: npm run dev -- --host
    2. Open at 844x390 (phone landscape)
    3. Click gear icon → Settings panel opens
       - Verify: panel width reasonable, not too narrow
       - Verify: all settings items visible without excessive scrolling
       - Verify: close button tappable
    4. Click ? icon → HowToPlay carousel opens
       - Verify: card width reasonable
       - Verify: slide content readable (icons, text)
       - Verify: swipe/arrows work
       - Verify: card doesn't overflow viewport height
    5. Play a full game to reach Winners screen
       - OR: temporarily lower sessionTargetScore to test faster
       - Verify: heading and scores readable
       - Verify: player rows visible and spaced
       - Verify: Play Again and Menu buttons tappable
    6. Test at wide viewport (1440x810) — verify all modals look good
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 38, or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds
- [ ] All modals render properly in landscape
- [ ] No fixed font sizes remain on key headings
- [ ] No viewport overflow
- [ ] All interactive elements tappable
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Phase 38 complete — all screens work in landscape
- Ready for Phase 39 (Cleanup & UAT)
</success_criteria>

<output>
After completion, create `.planning/phases/38-menu-screens/38-02-SUMMARY.md`
</output>
