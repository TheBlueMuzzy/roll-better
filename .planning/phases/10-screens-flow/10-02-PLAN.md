---
phase: 10-screens-flow
plan: 02
type: execute
---

<objective>
Create the Winners Screen — shown when a session ends (someone reaches 20 points).

Purpose: Currently sessionEnd just shows "Game Over! Score: X" in the HUD. This plan adds a proper results screen with all players ranked, winner highlighted, stats, and navigation to replay or return to menu.
Output: WinnersScreen.tsx component, session end transitions to winners screen.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-screens-flow/10-01-SUMMARY.md

# Key files to modify:
@src/App.tsx
@src/App.css
@src/components/HUD.tsx
@src/types/game.ts
@src/store/gameStore.ts

# Existing patterns:
@src/components/MainMenu.tsx
@src/components/Settings.tsx

**Tech stack available:** React, TypeScript, Zustand, CSS
**Established patterns:**
- Screen state: `screen: 'menu' | 'game' | 'winners'` in store (from 10-01)
- HTML overlays as siblings to Canvas
- PLAYER_COLORS array in gameStore.ts for player color coding
- PlayerProfileGroup pattern: avatar circle + score display

**Constraining decisions:**
- Phase 5: checkSessionEnd() returns true when any player.score >= sessionTargetScore (20)
- Phase 5: scoreRound applies points, applyHandicap adjusts dice counts
- Session end flow (App.tsx): roundEnd → checkSessionEnd() → setPhase('sessionEnd') after 500ms
- Players array has all final scores, names, colors, isAI, lockedDice counts
- currentRound tracks how many rounds were played

**Current sessionEnd behavior to change:**
- App.tsx lines 127-131: checkSessionEnd() → setPhase('sessionEnd') after 500ms delay
- HUD.tsx line 107-108: shows "Game Over! Score: {playerScore}" — this will be replaced by WinnersScreen
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WinnersScreen component</name>
  <files>src/components/WinnersScreen.tsx, src/App.css</files>
  <action>
1. Create src/components/WinnersScreen.tsx:
   - Full-screen HTML overlay (z-index 70, same as MainMenu)
   - Props: `onPlayAgain: () => void`, `onMenu: () => void`
   - Read from store: `players`, `currentRound`
   - Sort players by score descending to determine ranking
   - Display:
     - "Game Over" heading at top (~25% from top)
     - Winner announcement: "[Player Name] Wins!" with their color, or "You Win!" if human won. If tie, show "Tie!" with both names.
     - Ranked player list — each row shows:
       - Rank number (1st, 2nd, 3rd, 4th) or trophy icon for 1st
       - Player avatar circle (colored, with first letter of name) — reuse the same visual as PlayerProfileGroup but simpler (just circle + letter)
       - Player name
       - Score (bold, large)
       - Winner row gets a highlight/glow treatment (subtle gold border or background)
     - "Rounds played: {currentRound}" below the list
     - Two buttons at bottom:
       - "Play Again" — prominent, same style as menu Play button. Calls onPlayAgain.
       - "Menu" — secondary/outlined style. Calls onMenu.
   - Animation: player rows stagger in from bottom (CSS animation, 100ms delay each)
   - Style: dark background matching menu (#1a1a1a), clean typography

2. In App.css: Add winners screen styles:
   - `.winners-backdrop` — fixed full-screen, z-index 70, dark bg, flex column center
   - `.winners-heading` — large "Game Over" text
   - `.winners-announcement` — winner name, colored
   - `.winners-list` — ranked player rows container
   - `.winners-row` — flex row: rank + avatar + name + score
   - `.winners-row.winner` — highlighted (gold border-left or subtle glow)
   - `.winners-avatar` — small colored circle with letter (32-40px)
   - `.winners-rank` — rank number/trophy
   - `.winners-name` — player name
   - `.winners-score` — score value, bold
   - `.winners-rounds` — "Rounds played" text, muted
   - `.winners-actions` — button row at bottom
   - `.winners-play-again` — prominent button (reuse menu-play style)
   - `.winners-menu-btn` — secondary/outlined button
   - Stagger animation: `.winners-row { animation: slideUp 0.3s ease-out forwards; opacity: 0; }` with nth-child delays
  </action>
  <verify>Build passes (npm run build). WinnersScreen component exists with proper TypeScript types. CSS classes defined.</verify>
  <done>WinnersScreen.tsx renders ranked players with scores, winner highlight, round count, Play Again and Menu buttons. Stagger animation on rows.</done>
</task>

<task type="auto">
  <name>Task 2: Wire winners screen into session end flow</name>
  <files>src/App.tsx, src/components/HUD.tsx</files>
  <action>
1. In App.tsx:
   - Import WinnersScreen
   - Modify the roundEnd → sessionEnd flow: after setPhase('sessionEnd'), also set screen='winners' after a short delay (500ms after sessionEnd, so total 1000ms from roundEnd — gives time for the final score animation to play):
     ```
     // In the roundEnd useEffect where checkSessionEnd() is true:
     if (checkSessionEnd()) {
       const t1 = setTimeout(() => setPhase('sessionEnd'), 500);
       const t2 = setTimeout(() => setScreen('winners'), 1000);
       return () => { clearTimeout(t1); clearTimeout(t2); };
     }
     ```
   - Render WinnersScreen when screen === 'winners':
     ```
     {screen === 'winners' && (
       <WinnersScreen onPlayAgain={handlePlayAgain} onMenu={handleMenu} />
     )}
     ```
   - Add handlePlayAgain callback — replays with same settings:
     ```
     const handlePlayAgain = useCallback(() => {
       const state = useGameStore.getState();
       const playerCount = state.players.length;
       const aiDifficulty = state.players.find(p => p.isAI)?.difficulty || 'medium';
       initGame(playerCount, aiDifficulty);
       initRound();
       setScreen('game');
       // Spawn animation (same as handlePlay)
       const newState = useGameStore.getState();
       const humanPlayer = newState.players[0];
       if (humanPlayer && humanPlayer.poolSize > 0) {
         const spawnPositions = getSpawnPositions(humanPlayer.poolSize);
         setPoolSpawning(true, spawnPositions);
         const spawnDuration = 600 + humanPlayer.poolSize * 80 + 100;
         setTimeout(() => setPoolSpawning(false), spawnDuration);
       }
     }, [initGame, initRound, setScreen, setPoolSpawning]);
     ```
   - Add handleMenu callback:
     ```
     const handleMenu = useCallback(() => {
       setScreen('menu');
     }, [setScreen]);
     ```
   - Keep Canvas/3D scene rendered behind winners screen (the dice board visible behind the overlay gives nice visual context). Winners overlay is semi-transparent or has slight transparency at edges.

2. In HUD.tsx:
   - Remove or hide the "Game Over! Score: X" text for sessionEnd phase — the WinnersScreen handles this now. Change the sessionEnd case to show nothing (empty string) or a brief "Game Over!" without score:
     ```
     } else if (phase === 'sessionEnd') {
       statusText = '';
     }
     ```
   - The HUD remains visible behind the winners overlay but with no conflicting text.
  </action>
  <verify>Run `npm run dev`. Play a game to completion (can temporarily set sessionTargetScore to 2 for quick testing by editing gameStore.ts initialState). Winners screen appears after session ends. Play Again starts new game. Menu returns to main menu.</verify>
  <done>Session end triggers winners screen overlay. Ranked players shown correctly. Play Again replays with same settings. Menu returns to main menu. HUD doesn't show conflicting "Game Over" text.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Winners Screen that appears when a session ends, showing ranked players with scores, a winner highlight, and Play Again / Menu buttons.</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Start a game from the main menu (2 players, Easy AI for quick win)
    3. Play until someone reaches 20 points (or temporarily lower sessionTargetScore to 2 in gameStore.ts for faster testing)
    4. Verify: Winners screen appears with dark overlay
    5. Verify: Players ranked by score (winner at top)
    6. Verify: Winner row is highlighted (gold/glow treatment)
    7. Verify: Winner announcement shows correct name
    8. Verify: "Rounds played" count is correct
    9. Verify: Player rows stagger-animate in
    10. Tap "Play Again" — new game starts with same player count and difficulty
    11. Play to session end again, tap "Menu" — returns to main menu
    12. Verify: No "Game Over! Score: X" text visible in HUD behind the overlay
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Winners screen appears after session end
- [ ] Players ranked correctly by score
- [ ] Winner highlighted with visual distinction
- [ ] Play Again starts new game with same settings
- [ ] Menu returns to main menu
- [ ] No conflicting HUD text during sessionEnd
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- Winners screen shows correct rankings, scores, and round count
- Play Again and Menu navigation both work
- Session end → winners transition is smooth (no jarring jumps)
  </success_criteria>

<output>
After completion, create `.planning/phases/10-screens-flow/10-02-SUMMARY.md`
</output>
