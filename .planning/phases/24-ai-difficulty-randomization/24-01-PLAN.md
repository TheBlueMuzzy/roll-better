---
phase: 24-ai-difficulty-randomization
type: execute
---

<objective>
Remove manual AI difficulty selection and randomly assign Easy/Medium/Hard per AI at game start.

Purpose: Each AI opponent gets a random personality instead of all playing the same way. Makes games feel more varied without any player configuration needed.
Output: Every AI bot gets a random difficulty at fill time — both offline and online.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Key files:**
@src/store/gameStore.ts — initGame assigns difficulty to AI players (lines 337-380)
@src/types/game.ts — AIDifficulty type, GamePrefs.aiDifficulty field
@src/types/protocol.ts — start_game/game_starting messages carry aiDifficulty
@src/components/LobbyScreen.tsx — handleStart passes hardcoded 'hard' (line 154)
@src/hooks/useRoom.ts — startGame accepts aiDifficulty param
@src/hooks/useOnlineGame.ts — passes aiDifficulty to initGame (line 385-386)
@src/App.tsx — offline: passes gamePrefs.aiDifficulty to initGame (line 125), online: handleOnlineGameStart (line 159)
@party/server.ts — handleStartGame/handleRestartGame assign uniform difficulty to bots
@src/utils/aiDecision.ts — getAIUnlockDecision uses per-player difficulty (already supports mixed)

**Constraining decisions:**
- Phase 22: Difficulty selector already removed from main menu
- Phase 23: Settings panel cleaned up (no difficulty UI there either)

**Architecture note:**
- aiDecision.ts already reads `difficulty` from each Player object — mixed difficulties already work at the decision layer
- The change is purely about HOW difficulty gets assigned (random per-bot instead of uniform from a selector)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Randomize AI difficulty assignment everywhere</name>
  <files>src/store/gameStore.ts, src/types/game.ts, src/App.tsx, party/server.ts, src/types/protocol.ts, src/hooks/useRoom.ts, src/hooks/useOnlineGame.ts, src/components/LobbyScreen.tsx</files>
  <action>
**Create a shared helper** in `src/utils/aiDecision.ts` (where AIDifficulty already lives):

```ts
const DIFFICULTIES: AIDifficulty[] = ['easy', 'medium', 'hard'];
export function randomDifficulty(): AIDifficulty {
  return DIFFICULTIES[Math.floor(Math.random() * DIFFICULTIES.length)];
}
```

**gameStore.ts — initGame:**
- Remove `aiDifficulty` parameter (currently `AIDifficulty = 'medium'`)
- Change signature to `initGame: (playerCount: number, onlinePlayers?: ...)`
- For each AI player (both the `i !== 0` online path and the bot-filling path), call `randomDifficulty()` instead of using the uniform `aiDifficulty` param
- Import `randomDifficulty` from `../utils/aiDecision`

**types/game.ts — GamePrefs:**
- Remove the `aiDifficulty` field from `GamePrefs` interface (no longer user-configurable)

**gameStore.ts — initialState.gamePrefs:**
- Remove `aiDifficulty: 'medium'` from the gamePrefs initial value

**App.tsx:**
- Offline play (line ~125): Change `initGame(gamePrefs.playerCount, gamePrefs.aiDifficulty)` to `initGame(gamePrefs.playerCount)`
- Online play (handleOnlineGameStart, line ~159-169): Change `initGame(targetPlayers, difficulty, orderedPlayers)` to `initGame(targetPlayers, orderedPlayers)` — remove the difficulty cast/param entirely

**party/server.ts:**
- Add a local `randomDifficulty()` helper at the top (same logic — server can't import from src/utils, it's a separate bundle):
  ```ts
  const AI_DIFFICULTIES = ['easy', 'medium', 'hard'] as const;
  function randomDifficulty(): string {
    return AI_DIFFICULTIES[Math.floor(Math.random() * AI_DIFFICULTIES.length)];
  }
  ```
- **handleStartGame:** Remove `aiDifficulty` parameter. For each bot added, use `difficulty: randomDifficulty()` instead of the uniform param. Remove `aiDifficulty` from `ServerGameState` interface and from the state object.
- **handleRestartGame:** Same — use `randomDifficulty()` per bot instead of `this.gameState.aiDifficulty`. Remove aiDifficulty from rebuilt state.
- **serverHandleAIUnlocks:** Already reads `player.difficulty` — no change needed. Remove the `?? this.gameState.aiDifficulty` fallback (line 876) since every bot now has its own difficulty assigned at creation.
- Remove `aiDifficulty` from the `game_starting` broadcast message (lines 387, 478)
- Remove `aiDifficulty` from the `ServerGameState` interface (line 44)

**types/protocol.ts:**
- Remove `aiDifficulty` field from `StartGameMessage` and `GameStartingMessage` types

**useRoom.ts:**
- Remove `aiDifficulty` parameter from `startGame` function signature and the message payload
- Remove `aiDifficulty` from `GameStartData` interface

**useOnlineGame.ts:**
- Remove the `difficulty` extraction from `game_starting` handler (line 385-386)
- Change `store.initGame(msg.targetPlayers, difficulty, orderedPlayers)` to `store.initGame(msg.targetPlayers, orderedPlayers)`

**LobbyScreen.tsx:**
- `handleStart` (line 154): Change `room.startGame(total, 'hard')` to `room.startGame(total)`
- `onGameStart` prop type: Remove `aiDifficulty` parameter
- `useEffect` game start detection: Remove `aiDifficulty` from the onGameStart call

**LobbyScreen props + App.tsx handleOnlineGameStart:**
- Remove `aiDifficulty` from the callback signature in both the prop type and the handler
  </action>
  <verify>
- `npx tsc --noEmit` passes (no type errors from removed fields)
- `npm run build` succeeds
- Grep for `aiDifficulty` in src/ and party/ — should only appear in legacy comments or nowhere
  </verify>
  <done>
- No uniform aiDifficulty passed anywhere — each AI gets random difficulty at creation time
- GamePrefs no longer has aiDifficulty field
- Protocol messages no longer carry aiDifficulty
- Server assigns random per-bot difficulty
- Build passes cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Update tests and verify</name>
  <files>src/utils/aiDecision.test.ts</files>
  <action>
**aiDecision.test.ts:**
- Add a test for `randomDifficulty()`: call it 100 times, verify all results are one of 'easy', 'medium', 'hard', and that at least 2 of the 3 values appear (statistical — extremely unlikely to fail)

**Existing tests:**
- Review existing tests — they all pass explicit `difficulty` values in their test inputs, so they should still pass unchanged. No changes needed to existing tests.

**Build + type check:**
- Run `npx tsc --noEmit` to confirm no type errors
- Run `npx vitest run` (or whatever test runner) to confirm all tests pass
- Run `npm run build` to confirm production build works

**Final grep verification:**
- `grep -r "aiDifficulty" src/ party/` should return zero matches (confirming full cleanup)
  </action>
  <verify>
- `npx vitest run` — all tests pass including new randomDifficulty test
- `npm run build` — succeeds
- `grep -r "aiDifficulty" src/ party/` — no matches
  </verify>
  <done>
- All existing AI decision tests still pass
- New randomDifficulty test confirms uniform random selection
- No references to aiDifficulty remain in codebase
- Build clean
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npx tsc --noEmit` passes
- [ ] `npx vitest run` — all tests pass
- [ ] `npm run build` succeeds
- [ ] No `aiDifficulty` references remain in src/ or party/
- [ ] Offline game: start a game — each AI should get a random difficulty (visible in console if logging exists)
- [ ] Online game: start_game message no longer carries aiDifficulty
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors or build warnings introduced
- Each AI bot gets independently randomized difficulty
- No user-facing difficulty selection remains anywhere
  </success_criteria>

<output>
After completion, create `.planning/phases/24-ai-difficulty-randomization/24-01-SUMMARY.md`
</output>
