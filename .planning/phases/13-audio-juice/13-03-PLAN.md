---
phase: 13-audio-juice
plan: 03
type: execute
---

<objective>
Add UI and scoring sounds — the feedback layer for game events and player interactions.

Purpose: Score counting, round transitions, win moments, and UI interactions all get audio cues so the game communicates through sound.
Output: Complete audio coverage for all game events and UI interactions.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-audio-juice/13-01-SUMMARY.md
@.planning/phases/13-audio-juice/13-02-SUMMARY.md

# Key source files:
@src/utils/soundManager.ts
@src/components/HUD.tsx
@src/App.tsx
@src/store/gameStore.ts

**HUD.tsx score animation (line 37-79):**
- animateScore() runs 1500ms ease-out cubic
- Updates scoreRef.current.textContent per RAF frame
- Brief scale pulse at end
- Triggered when phase === 'scoring'

**App.tsx phase transitions:**
- locking → unlocking/scoring: 1000ms delay
- scoring → roundEnd: 2000ms delay
- roundEnd: pool exit (450ms) → goal transition (500ms) → pool spawn → idle (2000ms)
- sessionEnd: winners screen

**Game phases:** idle, rolling, locking, unlocking, scoring, roundEnd, sessionEnd

**UI interactions:**
- toggleUnlockSelection() — tap die to select/deselect for unlock
- handleTapRoll() — tap to roll
- Settings/H2P button clicks
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add score counting and win sounds</name>
  <files>src/utils/soundManager.ts, src/components/HUD.tsx, src/App.tsx</files>
  <action>
1. **SoundManager — add scoring sounds:**
   - `playScoreTick()` — tiny sine beep (1400Hz, 8ms, volume 0.2). For each point increment during counting.
   - `playScoreComplete()` — satisfying three-tone ascending chime (C5→E5→G5, ~523→659→784Hz, 60ms each, slight overlap, volume 0.4). Signals round score tallied.
   - `playWinFanfare()` — ascending arpeggio (C5→E5→G5→C6, 80ms each with 40ms gaps, volume 0.5). Plays when session winner declared.

2. **HUD.tsx — score tick integration:**
   - Inside animateScore's RAF loop, call `playScoreTick()` when the displayed score increments by 1 (track lastDisplayedScore, play tick each time Math.floor changes)
   - At the end of the animation (when progress >= 1), call `playScoreComplete()`
   - Throttle ticks to max ~10 per second (every ~100ms) to avoid audio spam for large scores

3. **App.tsx — win fanfare:**
   - When phase transitions to 'sessionEnd', call `playWinFanfare()`
   - Simple one-shot, no looping
  </action>
  <verify>Score a round — hear tick-tick-tick as score counts up, then a chime when counting finishes. Win a session — hear ascending fanfare. Volume slider controls all.</verify>
  <done>Score counting produces ticks per point. Score complete plays chime. Session win plays fanfare. All respect volume setting.</done>
</task>

<task type="auto">
  <name>Task 2: Add UI interaction and phase transition sounds</name>
  <files>src/utils/soundManager.ts, src/App.tsx, src/components/HUD.tsx, src/components/DicePool.tsx</files>
  <action>
1. **SoundManager — add UI sounds:**
   - `playUIClick()` — micro click (noise burst, 5ms, bandpass 3000Hz, volume 0.15). Generic button/selection sound.
   - `playSelectDie()` — soft select tone (sine 900Hz, 15ms, volume 0.2). For die selection in unlock phase.
   - `playDeselectDie()` — lower deselect tone (sine 600Hz, 15ms, volume 0.15). For deselecting.
   - `playRoundStart()` — soft rising tone (sine sweep 400→600Hz over 200ms, volume 0.2). New round beginning.
   - `playNoMatch()` — flat two-tone (sine 400Hz then 350Hz, 60ms each, volume 0.2). "No matches" feedback.

2. **Wire UI sounds:**
   - DicePool.tsx or Scene.tsx: when unlock selection toggled, play `playSelectDie()` or `playDeselectDie()` based on whether die was added/removed from selection. Gate on unlock phase.
   - App.tsx: when phase transitions to 'idle' after roundEnd, play `playRoundStart()`.
   - App.tsx or HUD.tsx: when phase === 'locking' and no new locks found, play `playNoMatch()`.
   - Settings/H2P open/close: call `playUIClick()` on button taps.

3. **Keep it subtle:** UI sounds should be barely noticeable — they're felt more than heard. If in doubt, make it quieter. The dice collision sounds from Plan 01 should be the loudest things in the game.
  </action>
  <verify>Tap to roll — dice sounds dominate. Unlock selection — hear soft select/deselect tones. No matches — hear flat tone. New round starts — hear rising tone. Settings/H2P buttons — hear micro click. Everything respects volume slider.</verify>
  <done>All UI interactions have audio feedback. Phase transitions have sound cues. Sound hierarchy: dice impacts > game events > UI clicks. Volume control works for everything.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Score counting: ticks per point + completion chime
- [ ] Win fanfare plays on session end
- [ ] Die selection: select/deselect tones
- [ ] No-match feedback tone
- [ ] Round start tone
- [ ] UI button clicks
- [ ] Volume slider controls all sounds
- [ ] Sound hierarchy feels right (dice loudest, UI quietest)
- [ ] No audio spam or glitches during rapid play
</verification>

<success_criteria>
- Complete audio coverage for all game events
- Sound hierarchy: physics > game events > UI
- All sounds respect volume setting
- No annoying or spammy sounds
- Game feels "alive" with audio
</success_criteria>

<output>
After completion, create `.planning/phases/13-audio-juice/13-03-SUMMARY.md`
</output>
